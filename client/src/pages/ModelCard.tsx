/** Community Wayfinding: the model card makes PxK’s prototype logic, limits, and human boundaries plainly inspectable. */
import { PathwayRail } from "@/components/PathwayRail";
import { SectionHeading } from "@/components/UiPrimitives";
import { BrainCircuit, CheckCheck, CircleX, DatabaseZap, Eye, ShieldCheck } from "lucide-react";
import { Link } from "wouter";

const facts = [{ title: "Uses", copy: "The selected answers from a 12-question respiratory, digestive, or dental workflow.", icon: DatabaseZap }, { title: "Produces", copy: "A potential specialty route, a factor trace, and a non-diagnostic priority-review cue when configured answers are present.", icon: BrainCircuit }, { title: "Shows", copy: "The exact answer categories used by the prototype logic, so the route is inspectable rather than a black box.", icon: Eye }];
export default function ModelCard() { return <main className="model-page"><div className="container py-8 lg:py-12"><div className="dashboard-topline"><p className="eyebrow">Model card · PxK Route Engine v0.1</p><Link className="text-button" href="/analytics">Return to analytics</Link></div><div className="mt-7 grid gap-10 lg:grid-cols-[1fr_.7fr]"><div><h1 className="display-title text-5xl">A transparent prototype engine, not a medical diagnosis model.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[var(--muted-ink)]">PxK Route Engine v0.1 is a deterministic, client-side decision-support demonstration. It makes its routing rationale visible so a user can understand which selected responses contributed to a potential next clinical conversation.</p></div><div className="model-identity"><BrainCircuit /><p className="eyebrow">Engine identity</p><h2>PxK Route Engine<br />v0.1</h2><span>Browser-local · deterministic · demonstration only</span></div></div><div className="mt-12"><PathwayRail active={2} compact /></div><section className="model-facts">{facts.map((item) => { const Icon = item.icon; return <article key={item.title}><Icon /><p className="eyebrow">The engine {item.title.toLowerCase()}</p><h2>{item.title}</h2><p>{item.copy}</p></article>; })}</section><section className="model-boundaries"><div><p className="eyebrow">Designed boundaries</p><h2>What the prototype can and cannot do.</h2></div><div className="boundary-columns"><article><CheckCheck /><h3>It can</h3><ul><li>Structure patient-reported answers.</li><li>Provide a transparent potential specialty route.</li><li>Mark configured answers for priority clinical review.</li><li>Preserve the routing rationale in local history.</li></ul></article><article><CircleX /><h3>It cannot</h3><ul><li>Diagnose a disease or condition.</li><li>Recommend treatment, medication, or dosage.</li><li>Guarantee a clinical outcome or care priority.</li><li>Replace a qualified healthcare professional.</li></ul></article></div></section><section className="model-governance"><ShieldCheck /><div><p className="eyebrow">Before real-world use</p><h2>Clinical governance would be essential.</h2><p>A deployment-ready system would need clinically curated and de-identified data, specialist review of routing criteria, prospective validation, consent controls, privacy engineering, audit trails, and a formal escalation process. Those elements are intentionally outside this frontend demonstration.</p></div></section></div></main>; }
